# Scripts

This folder contains project utility scripts.

## Seed

- `scripts/seed.js`: Generates SQL seed data from `src/data/products.ts`.
- Use `npm run seed` to execute the seeding script.

## Notes

Test and debug files have been removed from the repository root to keep the production repo clean.
